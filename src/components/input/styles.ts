import styled from 'styled-components'

export const InputContainer = styled.div`
    position: relative;

    .MuiFormHelperText-root {
        position: absolute;
        margin: 3.7rem 0 0;
    }
`
